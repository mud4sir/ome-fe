import React from 'react';
import AddNewItemProps from '../types/AddNewItemProps';

const SellNewItem: React.FC<AddNewItemProps> = (props) => {
    return <div>In progress...</div>;
};
export default SellNewItem;
