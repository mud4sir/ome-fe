import React from 'react';
import Navigation from './Navigation';
import Layout from './Layout';
import HomeProps from '../types/HomeProps';

const Home: React.FC<HomeProps> = (props) => {
    return (
        <Layout>
            <h2>Home Page is in progress...</h2>
        </Layout>
    );
};
export default Home;
